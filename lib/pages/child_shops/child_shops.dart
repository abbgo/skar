import 'package:flutter/material.dart';

class ChildShopsPage extends StatelessWidget {
  const ChildShopsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Child shops'),
      ),
    );
  }
}
